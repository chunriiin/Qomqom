<html style="height: 100%;">
<div style="display: flex;">
  <div class="right-box m-0 mh-100 text-center d-md-flex d-none" style="height: 100vh;width: 50%;background:#FF0033;padding:3.0em; align-items:center">
    <strong class="text-white" style="font-size: 2.0em; margin-top: -30%;" >Qomqomへようこそ</strong>
  </div>

  <div class="mh-100 p-5"style="width: 50%;">
    <div>
      <strong class="" style="font-size: 1.5em;">興味がある話題から<br>世界に友達をつくろう</strong>
    </div>
    <div class="mt-5">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="field">
        <label for="username">ユーザーネーム</label><br />
        <%= f.text_field :username, autofocus: true, autocomplete: "" %>
      </div>

      <div class="field">
      <label for="email">メールアドレス</label><br />
        <%= f.email_field :email, autocomplete: "" %>
      </div>

      <div class="form-group"><%= f.file_field :image, class: "form-control form-control-white" %></div>

      <div class="field">
      <label for="password">パスワード</label><br />
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "" %>
      </div>

      <div class="field">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "" %>
      </div>

      <div class="actions">
        <%= f.submit "始める" , class:"btn btn-outline-danger my-2 my-sm-0" %>
      </div>
    <% end %>
  </div>
  <div class="mt-3">アカウントをすでにお持ちですか？</div>
  <%= render "devise/shared/links" %>
  </div>
  
</div>
</html>
